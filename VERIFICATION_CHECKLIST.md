# ✅ چک‌لیست تست و تایید عملکرد پروژه

این فایل برای اطمینان از عملکرد صحیح تمام قسمت‌های پروژه است.

---

## 🗄️ بخش 1: تست دیتابیس و Schema

### ✅ بررسی جداول
در SQL Editor Supabase این کوئری را اجرا کنید:

```sql
-- بررسی تمام جداول
SELECT 
    table_name,
    (SELECT COUNT(*) FROM information_schema.columns WHERE table_name = t.table_name) as column_count
FROM information_schema.tables t
WHERE table_schema = 'public'
AND table_type = 'BASE TABLE'
ORDER BY table_name;
```

**نتیجه مورد انتظار:**
- ✅ users (7 columns)
- ✅ category (4 columns)
- ✅ news (22 columns)
- ✅ video (6 columns)
- ✅ comments (8 columns)

### ✅ بررسی Relations
```sql
-- بررسی Foreign Keys
SELECT
    tc.table_name,
    kcu.column_name,
    ccu.table_name AS foreign_table_name,
    ccu.column_name AS foreign_column_name
FROM information_schema.table_constraints AS tc
JOIN information_schema.key_column_usage AS kcu
    ON tc.constraint_name = kcu.constraint_name
JOIN information_schema.constraint_column_usage AS ccu
    ON ccu.constraint_name = tc.constraint_name
WHERE tc.constraint_type = 'FOREIGN KEY'
ORDER BY tc.table_name;
```

**نتیجه مورد انتظار:**
- ✅ news.userId → users.id
- ✅ news.catId → category.id
- ✅ comments.newsId → news.id

### ✅ بررسی RLS Policies
```sql
-- بررسی تمام policies
SELECT 
    schemaname,
    tablename,
    policyname,
    permissive,
    roles,
    cmd
FROM pg_policies
WHERE schemaname = 'public'
ORDER BY tablename, policyname;
```

**نتیجه مورد انتظار:**
حداقل باید این policies وجود داشته باشند:
- ✅ users: 3 policies
- ✅ category: 4 policies
- ✅ news: 2 policies
- ✅ video: 2 policies
- ✅ comments: 4 policies

---

## 📦 بخش 2: تست Storage Buckets

### ✅ بررسی وجود Buckets
```sql
SELECT 
    id,
    name,
    public,
    file_size_limit,
    allowed_mime_types
FROM storage.buckets
WHERE id IN ('news-images', 'videos', 'profile-images');
```

**نتیجه مورد انتظار:**
```
id              | name            | public | file_size_limit | allowed_mime_types
----------------|-----------------|--------|-----------------|--------------------
news-images     | news-images     | true   | 5242880        | {image/jpeg,...}
videos          | videos          | true   | 104857600      | {video/mp4,...}
profile-images  | profile-images  | true   | 2097152        | {image/jpeg,...}
```

### ✅ بررسی Storage Policies
```sql
SELECT 
    policyname,
    cmd
FROM pg_policies
WHERE schemaname = 'storage'
AND tablename = 'objects'
ORDER BY policyname;
```

**نتیجه مورد انتظار:**
حداقل 12 policy (4 برای هر bucket: SELECT, INSERT, UPDATE, DELETE)

---

## 🔐 بخش 3: تست Authentication

### ✅ تست ثبت‌نام کاربر جدید
1. به صفحه ثبت‌نام بروید (در پنل ادمین)
2. اطلاعات زیر را وارد کنید:
   ```
   Name: Test User
   Email: test@example.com
   Password: Test123456!
   Confirm Password: Test123456!
   Is Admin: true/false
   ```
3. روی "ثبت‌نام" کلیک کنید

**نتیجه مورد انتظار:**
- ✅ پیام "کاربر با موفقیت ساخته شد" نمایش داده شود
- ✅ به صفحه لیست کاربران هدایت شوید
- ✅ کاربر جدید در لیست باشد

### ✅ تست ورود
1. از حساب فعلی خارج شوید
2. با اطلاعات کاربر ایجاد شده وارد شوید

**نتیجه مورد انتظار:**
- ✅ پیام "خوش آمدید" نمایش داده شود
- ✅ به داشبورد ادمین هدایت شوید
- ✅ نام کاربر در نوار بالا نمایش داده شود

### ✅ تست خروج
1. روی دکمه خروج کلیک کنید

**نتیجه مورد انتظار:**
- ✅ پیام "خارج شدید" نمایش داده شود
- ✅ به صفحه اصلی هدایت شوید

---

## 📁 بخش 4: تست CRUD دسته‌بندی

### ✅ ایجاد دسته‌بندی
1. به "مدیریت دسته‌بندی" → "افزودن دسته" بروید
2. نام دسته را وارد کنید: "تست دسته‌بندی"
3. روی "ثبت" کلیک کنید

**نتیجه مورد انتظار:**
- ✅ پیام "دسته بندی ساخته شد" نمایش داده شود
- ✅ به صفحه لیست دسته‌ها هدایت شوید
- ✅ دسته جدید در لیست باشد

### ✅ ویرایش دسته‌بندی
1. روی دکمه "ویرایش" دسته کلیک کنید
2. نام را تغییر دهید به: "تست ویرایش شده"
3. روی "ذخیره" کلیک کنید

**نتیجه مورد انتظار:**
- ✅ پیام "دسته بندی ویرایش شد" نمایش داده شود
- ✅ نام جدید در لیست نمایش داده شود

### ✅ حذف دسته‌بندی
1. روی دکمه "حذف" کلیک کنید
2. تایید کنید

**نتیجه مورد انتظار:**
- ✅ پیام "دسته بندی حذف شد" نمایش داده شود
- ✅ دسته از لیست حذف شود

---

## 📰 بخش 5: تست CRUD اخبار (مهم‌ترین بخش)

### ✅ ایجاد خبر جدید
1. به "مدیریت اخبار" → "افزودن خبر" بروید
2. اطلاعات را پر کنید:
   ```
   عنوان: خبر تستی
   توضیحات: این یک خبر تستی است
   دسته‌بندی: یکی از دسته‌های موجود
   عکس اصلی: فایلی با حجم کمتر از 5MB انتخاب کنید
   ```
3. (اختیاری) عکس‌های اضافی و ویدیو اضافه کنید
4. روی "آماده انتشار" کلیک کنید

**نتیجه مورد انتظار:**
- ✅ پیام "خبر با موفقیت ایجاد شد" نمایش داده شود
- ✅ به صفحه لیست اخبار هدایت شوید
- ✅ خبر جدید با عکس در لیست باشد

**بررسی در Supabase:**
```sql
-- بررسی خبر ایجاد شده
SELECT id, title, image, url, "userId", "catId" 
FROM news 
WHERE title = 'خبر تستی';
```

**نتیجه مورد انتظار:**
- ✅ `image` نباید NULL باشد
- ✅ `url` نباید NULL باشد
- ✅ `image` باید شبیه این باشد: `https://xxxxx.supabase.co/storage/v1/object/public/news-images/...`

**بررسی فایل در Storage:**
1. به Supabase → Storage → news-images بروید
2. فایل آپلود شده را پیدا کنید
3. روی آن کلیک کنید و "Get URL" را انتخاب کنید

**نتیجه مورد انتظار:**
- ✅ فایل در bucket موجود باشد
- ✅ URL قابل دسترسی باشد

### ✅ نمایش خبر در صفحه اصلی
1. به صفحه اصلی سایت بروید
2. بررسی کنید که خبر تستی نمایش داده می‌شود

**نتیجه مورد انتظار:**
- ✅ خبر در بخش "آخرین اخبار" نمایش داده شود
- ✅ عکس خبر به درستی لود شود (نه broken image)
- ✅ اطلاعات خبر (عنوان، دسته‌بندی، نویسنده) صحیح باشد

### ✅ نمایش جزئیات خبر
1. روی خبر تستی کلیک کنید
2. صفحه جزئیات باز شود

**نتیجه مورد انتظار:**
- ✅ عنوان خبر نمایش داده شود
- ✅ عکس اصلی نمایش داده شود
- ✅ توضیحات کامل نمایش داده شود
- ✅ اگر ویدیو دارد، نمایش داده شود
- ✅ اگر عکس‌های اضافی دارد، نمایش داده شود
- ✅ تعداد بازدید +1 شود

**بررسی افزایش بازدید:**
```sql
SELECT id, title, "numViews" 
FROM news 
WHERE title = 'خبر تستی';
-- numViews باید حداقل 1 باشد
```

### ✅ ویرایش خبر
1. در پنل ادمین، روی ویرایش خبر کلیک کنید
2. عنوان را تغییر دهید به: "خبر ویرایش شده"
3. (اختیاری) عکس جدید آپلود کنید
4. روی "ذخیره" کلیک کنید

**نتیجه مورد انتظار:**
- ✅ پیام "خبر با موفقیت ویرایش شد" نمایش داده شود
- ✅ تغییرات در لیست اخبار اعمال شده باشد
- ✅ اگر عکس جدید آپلود شد، عکس قدیمی جایگزین شود

### ✅ حذف خبر
1. روی دکمه حذف کلیک کنید
2. تایید کنید

**نتیجه مورد انتظار:**
- ✅ پیام "خبر با موفقیت حذف شد" نمایش داده شود
- ✅ خبر از لیست حذف شود
- ✅ خبر از صفحه اصلی نیز حذف شود

---

## 🎥 بخش 6: تست ویدیوها

### ✅ آپلود ویدیو
1. به "مدیریت ویدیو" بروید
2. عنوان، توضیحات و فایل ویدیو را انتخاب کنید
3. روی "ثبت" کلیک کنید

**نتیجه مورد انتظار:**
- ✅ ویدیو با موفقیت آپلود شود
- ✅ در صفحه اصلی نمایش داده شود

---

## 💬 بخش 7: تست نظرات

### ✅ ثبت نظر
1. به صفحه جزئیات یک خبر بروید
2. فرم نظر را پر کنید
3. روی "ارسال" کلیک کنید

**نتیجه مورد انتظار:**
- ✅ پیام "نظر شما ثبت شد" نمایش داده شود
- ✅ نظر در بخش نظرات نمایش داده شود (بعد از تایید ادمین)

### ✅ مدیریت نظرات در پنل ادمین
1. به "مدیریت نظرات" بروید
2. نظر تست را پیدا کنید
3. آن را فعال/غیرفعال کنید

**نتیجه مورد انتظار:**
- ✅ وضعیت نظر تغییر کند
- ✅ اگر فعال شد، در صفحه خبر نمایش داده شود

---

## ⚡ بخش 8: تست عملکرد و سرعت

### ✅ تست سرعت بارگذاری صفحه اصلی
1. صفحه را Refresh کنید و Network Tab را باز کنید
2. زمان بارگذاری را بررسی کنید

**نتیجه مورد انتظار:**
- ✅ صفحه در کمتر از 3 ثانیه لود شود
- ✅ تصاویر به صورت lazy load شوند
- ✅ هیچ 404 error نباشد

### ✅ تست Pagination
1. به پنل ادمین → لیست اخبار بروید
2. اگر بیش از 10 خبر دارید، دکمه "بعدی" نمایش داده شود

**نتیجه مورد انتظار:**
- ✅ صفحه‌بندی کار کند
- ✅ در هر صفحه فقط 10 خبر نمایش داده شود

### ✅ تست جستجو
1. در لیست اخبار، چیزی در باکس جستجو تایپ کنید

**نتیجه مورد انتظار:**
- ✅ فیلتر کردن فوری کار کند
- ✅ نتایج مطابق با جستجو باشد

---

## 🔒 بخش 9: تست امنیت

### ✅ تست دسترسی بدون لاگین
1. خارج شوید
2. سعی کنید به URL پنل ادمین بروید: `/admin-dashboard`

**نتیجه مورد انتظار:**
- ✅ به صفحه لاگین redirect شوید

### ✅ تست RLS
در SQL Editor این کوئری را اجرا کنید:

```sql
-- بدون authentication تلاش برای insert
SET ROLE anon;
INSERT INTO news (title, description, "userId", "catId")
VALUES ('Hack Test', 'Test', '00000000-0000-0000-0000-000000000000', 1);
-- باید خطا دهد
```

**نتیجه مورد انتظار:**
- ✅ خطای permission denied دریافت کنید

---

## 📊 بخش 10: مانیتورینگ

### ✅ بررسی Console Errors
1. Console مرورگر را باز کنید
2. در تمام صفحات بگردید

**نتیجه مورد انتظار:**
- ✅ هیچ خطای قرمز نباشد
- ✅ فقط warning های غیرمهم باشد

### ✅ بررسی Network Errors
1. Network Tab را باز کنید
2. صفحات مختلف را بررسی کنید

**نتیجه مورد انتظار:**
- ✅ هیچ 404 نباشد
- ✅ هیچ 500 نباشد
- ✅ همه request های Supabase موفق باشند (200 یا 201)

---

## ✅ نتیجه نهایی

اگر تمام موارد بالا را پاس کردید، تبریک! 🎉

پروژه شما آماده Production است.

### گزارش مشکلات
اگر هر کدام از تست‌ها fail شد:

1. خطا را در Console/Network کپی کنید
2. لاگ Supabase را بررسی کنید
3. به راهنمای [SUPABASE_DEPLOYMENT_GUIDE.md](SUPABASE_DEPLOYMENT_GUIDE.md) مراجعه کنید
4. از بخش "رفع مشکلات رایج" استفاده کنید

---

## 📝 Template گزارش تست

```markdown
## گزارش تست - تاریخ: [DATE]

### محیط تست:
- [ ] Development (localhost)
- [ ] Production (Netlify)

### نتایج:
- Authentication: ✅ / ❌
- CRUD Categories: ✅ / ❌
- CRUD News: ✅ / ❌
- Image Upload: ✅ / ❌
- Video Upload: ✅ / ❌
- Comments: ✅ / ❌
- Performance: ✅ / ❌
- Security: ✅ / ❌

### مشکلات یافت شده:
1. [توضیح مشکل]
2. [توضیح مشکل]

### اقدامات انجام شده:
1. [اقدام]
2. [اقدام]

### وضعیت نهایی:
- [ ] آماده Production
- [ ] نیاز به رفع مشکلات
```
